{
  "name": "aiengjobs",
  "private": true,
  "version": "0.0.0",
  "description": "frontierroles.com — static job board (GitHub Pages) + nightly ingestion engine (GitHub Actions)",
  "workspaces": [
    "shared",
    "site",
    "engine",
    "mcp"
  ],
  "scripts": {
    "build": "npm run build -w @aiengjobs/site",
    "dev": "npm run dev -w @aiengjobs/site",
    "typecheck": "npm run typecheck -w @aiengjobs/engine && npm run check -w @aiengjobs/site && npm run typecheck -w @aiengjobs/mcp",
    "test": "vitest run",
    "snapshot:fetch": "git fetch --depth=1 origin snapshot && git cat-file blob FETCH_HEAD:snapshot.json > site/src/data/snapshot.json && echo 'Fetched site/src/data/snapshot.json'"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "vitest": "^3.0.0"
  }
}
