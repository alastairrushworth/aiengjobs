{
  "name": "@aiengjobs/mcp",
  "version": "0.1.0",
  "type": "module",
  "description": "MCP server exposing frontierroles.com — AI engineering roles as agent tools.",
  "bin": {
    "frontierroles-mcp": "./dist/stdio.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/stdio.ts",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.125.0"
  }
}
